{
  "scripts": {
    "start": "npm run build && node server/index.js",
    "lint": "npx eslint --fix ./public/src",
    "build": "build.sh"
  },
  "dependencies": {
    "handlebars": "^4.7.8",
    "ws": "^8.16.0"
  }
}
